import React from 'react'
import { View, Text } from 'react-native'

const Configuration = () => {
    return (
        <View>
            <Text> Config </Text>
        </View>
    );
}

export default Configuration;
